Exercise I: Set a GPU device

Note: The OpenACC code is stored in the folder "setDevice_acc"
      The OpenMP code is stored in te folder "setDevice_omp"

1. Implement either OpenACC or OpenMP functions that enable assigning each MPI rank to a GPU device.

1.1 Compile and run the code as follows:

To compile: "./compile.sh"

To submit a job: "sbatch script.slurm" 
